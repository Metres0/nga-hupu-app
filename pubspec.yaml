name: nga_hupu_app
description: NGA镜像站 - 液态玻璃阅读器 (Flutter/Android)
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.6.0
  flutter: ">=3.27.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  cached_network_image: ^3.4.0
  shared_preferences: ^2.3.0
  provider: ^6.1.0
  url_launcher: ^6.3.0
  intl: ^0.19.0
  photo_view: ^0.15.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
